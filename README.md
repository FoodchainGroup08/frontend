
  # FoodChain

  This is a code bundle for FoodChain. The original project is available at https://www.figma.com/design/UfhFKcgwAO70zP2Ty91Ix0/FoodChain.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  